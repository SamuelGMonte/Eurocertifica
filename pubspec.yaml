name: eurocertifica_web
description: Eurocertifica course platform rebuilt in Flutter with clean architecture.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  shared_preferences: ^2.3.2
  get_it: ^8.0.0
  dio: ^5.7.0
  provider: ^6.1.5
  mockito: ^5.4.0
  build_runner: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
